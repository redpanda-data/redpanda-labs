= [Step title: imperative verb phrase, for example Start the environment]
// Step page of the schema-registry-migration solution. The file stem is the step id and must appear
// in :page-solution-steps: on index.adoc. Rename this file to the step id.
:page-layout: solution-step
:description: [One sentence: what the reader does in this step and what they have at the end.]
// Optional, whole minutes for this step
:page-solution-step-duration: 5

// 1-2 sentences. What the reader does and why it is the next thing to do.
[What does the reader do in this step, and why now?]

== What you do

. [Instruction 1]
+
// Never type a command into a page. Put it in steps/<step-id>/commands.sh inside a
// tagged region and include the region; the Doc Detective spec runs the same region.
[source,bash]
----
include::example$steps/step/commands.sh[tag=run]
----
+
// The output is captured by tools/capture-expected.sh into steps/<step-id>/expected/<tag>.txt
// and included right after the command that produced it. Never type an expected output.
.Expected output
[source,text]
----
include::example$steps/step/expected/run.txt[]
----

. [Instruction 2]
+
// Show code from the solution directory, never paste it. Tag the region in the source
// file with // tag::name[] ... // end::name[] and include only that region.
[source,yaml]
----
include::example$docker-compose.yml[tags=redpanda]
----

== Why

[Explain the design decision behind this step in the terms of the business problem. Would this paragraph be true in any solution that uses the feature? Then it belongs in Product Docs: link it instead.]

== Why Redpanda

[Which Redpanda capability makes this step simple or possible, and what would it cost to do without it. Link the canonical page.]

== What happens inside

[What Redpanda does when the reader runs the commands: partitions, offsets, consumer groups, schema lookups. Keep it to the behaviour this step exercises.]

== Verify

// The last command block of the step is its Verify command; its expected output
// is captured like every other.
Run:

[source,bash]
----
include::example$steps/step/commands.sh[tag=verify]
----

Expected output:

[.solution-verify]
[source,text]
----
include::example$steps/step/expected/verify.txt[]
----

[If the output differs: the one or two most likely causes and how to fix them.]

== In production

[.production-note]
[One or two sentences: what changes about this step in a production deployment, with a link to the Production considerations row on the overview or the relevant Product Docs page.]

// Build-along files for this step. Every file the reader needs that is not shown in
// full on this page must be listed here as a public attachment.
.Files for this step
* link:{attachmentsdir}/docker-compose.yml[docker-compose.yml]
* link:{attachmentsdir}/env.example[env.example] (save it as `.env`)
* link:{attachmentsdir}/Makefile.mk[Makefile.mk] (save it as `Makefile`)
