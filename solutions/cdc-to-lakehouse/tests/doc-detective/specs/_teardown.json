{
  "specId": "_teardown",
  "description": "Tear the CDC to lakehouse stack down after every step spec has run (afterAll in .doc-detective.json). The compose profiles are named so the change data capture containers go too.",
  "tests": [
    {
      "testId": "_teardown",
      "steps": [
        {
          "runShell": {
            "command": "make clean",
            "timeout": 180000
          }
        }
      ]
    }
  ]
}
