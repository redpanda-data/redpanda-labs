{
  "specId": "_teardown",
  "description": "Tear Disaster Recovery Shadowing down after every step spec has run (afterAll in .doc-detective.json). `make clean` also deletes the facts the walkthrough recorded under state/.",
  "tests": [
    {
      "testId": "_teardown",
      "steps": [
        {
          "runShell": {
            "command": "make clean",
            "timeout": 180000
          }
        }
      ]
    }
  ]
}
