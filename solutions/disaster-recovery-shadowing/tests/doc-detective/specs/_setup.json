{
  "specId": "_setup",
  "description": "Start the Disaster Recovery Shadowing stack once before the step specs run (beforeAny in .doc-detective.json). Nothing is seeded here: the topic, the shadow link, the records, the outage, and the failover are all created by the step specs themselves, in step order, exactly as the pages show them. Never selected as a step spec.",
  "tests": [
    {
      "testId": "_setup",
      "steps": [
        {
          "runShell": {
            "command": "make up",
            "timeout": 900000
          }
        }
      ]
    }
  ]
}
