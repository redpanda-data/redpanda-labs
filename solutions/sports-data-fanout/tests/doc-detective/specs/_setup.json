{
  "specId": "_setup",
  "description": "Start the sports data fan-out stack once before the step specs run (beforeAny in .doc-detective.json). Topics, schemas and feed data are created by the step specs themselves, in step order, so nothing is pre-seeded here. Never selected as a step spec.",
  "tests": [
    {
      "testId": "_setup",
      "steps": [
        {
          "runShell": {
            "command": "make up",
            "timeout": 600000
          }
        }
      ]
    }
  ]
}
