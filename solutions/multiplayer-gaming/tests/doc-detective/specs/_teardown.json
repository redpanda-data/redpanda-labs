{
  "specId": "_teardown",
  "description": "Tear Multiplayer Gaming down after every step spec has run (afterAll in .doc-detective.json).",
  "tests": [
    {
      "testId": "_teardown",
      "steps": [
        {
          "runShell": {
            "command": "rm -f before.txt after.txt; make clean",
            "timeout": 120000
          }
        }
      ]
    }
  ]
}
