{
  "specId": "produce-player-events",
  "description": "Replays the 'produce-player-events' page: make seed waits for the cap, then the stats show exactly SIM_EVENTS_MAX acknowledged.",
  "contentPath": "docs/modules/multiplayer-gaming/pages/produce-player-events.adoc",
  "tests": [
    {
      "testId": "produce-player-events",
      "description": "The simulator produced exactly 3000 events and stopped",
      "steps": [
        {
          "runShell": {
            "command": "make seed",
            "timeout": 400000
          }
        },
        {
          "runShell": {
            "command": "docker compose exec -T rpk rpk topic consume game.player-events -p 1 -o start -n 1 --use-schema-registry=value",
            "timeout": 60000,
            "stdio": "/eventId/"
          }
        },
        {
          "runShell": {
            "command": "curl -s http://localhost:8090/stats | grep -E '\"state\"|\"acked_total\"'",
            "timeout": 60000,
            "stdio": "/\"state\": \"capped\",[\\s\\S]*\"acked_total\": 3000/"
          }
        }
      ]
    }
  ]
}
