{
  "specId": "step",
  "description": "Replays the Verify section of the 'step' page of Multiplayer Gaming. Rename this file, its specId, and its testId to the step id. One spec per step, same id as the page stem.",
  "contentPath": "docs/modules/multiplayer-gaming/pages/step.adoc",
  "tests": [
    {
      "testId": "step",
      "description": "The topic exists with the documented partition count",
      "steps": [
        {
          "runShell": {
            "command": "docker compose exec -T rpk rpk topic list",
            "stdio": "/multiplayer-gaming\\.events\\s+3\\s+1/",
            "timeout": 60000
          }
        }
      ]
    }
  ]
}
