{
  "specId": "detect-achievements",
  "description": "Replays the 'detect-achievements' page: unlocks are on game.achievements and the service reports the documented seed-42 counts.",
  "contentPath": "docs/modules/multiplayer-gaming/pages/detect-achievements.adoc",
  "tests": [
    {
      "testId": "detect-achievements",
      "description": "The achievements service tracked 24 players and unlocked the documented achievements",
      "steps": [
        {
          "runShell": {
            "command": "docker compose exec -T rpk rpk topic consume game.achievements -o start -n 1 --use-schema-registry=value",
            "timeout": 60000,
            "stdio": "/achievementUnlocked/"
          }
        },
        {
          "runShell": {
            "command": "curl -s http://localhost:3010/healthz | grep -E 'players_tracked|hot_streak|first_win'",
            "timeout": 60000,
            "stdio": "/\"players_tracked\": 24,[\\s\\S]*\"first_win\": 22,[\\s\\S]*\"hot_streak\": 374/"
          }
        }
      ]
    }
  ]
}
